{
  "name": "optsolv-time-tracker-azdo-extension",
  "version": "1.1.12",
  "private": true,
  "description": "OptSolv Time Tracker — Azure DevOps Extension",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "typecheck:test": "tsc -p tsconfig.test.json",
    "test": "npm run typecheck:test && node scripts/run-tests.mjs",
    "package": "npm run test && npm run build && tfx extension create --manifest-globs vss-extension.json --output-path dist/vsix"
  },
  "dependencies": {
    "azure-devops-extension-api": "^4.0.4",
    "azure-devops-extension-sdk": "^4.0.2",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.0.0",
    "esbuild": "^0.25.12",
    "tfx-cli": "^0.16.0",
    "typescript": "^5.8.0",
    "vite": "^6.0.0"
  }
}
